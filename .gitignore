node_modules
build
coverage
.vscode
.DS_STORE
.env
tmp
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*
logs
*.log
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*
.yarn-integrity


library mgt
routes:get:get all,get by id,route parameter,search(category)
post:many,put,patch -> json input
update:many  -> path parameter
delete:many by id -> query parameter
validator from db , pk,fk in db , 
request -> validator

middleware -> api key auth

controller -> validation,repository calling , return - try ,catch(error handling) 

repository -> DB operation,logical operation,service calling(third party),return response to controller,

min -> 3 table with constraint

routes,request,response,context,validator,middleware,controller,models,migration,orm methods,query builder,error handling-try catch
error in repo handled in controller -> return error 

multiple update,delete

{
  "title": "Clean Code",
  "author": "Robert C. Martin",
  "copies_total": 5,
  "copies_available": 5
}

{
  "title": "Clean Code - Updated Edition",
  "copies_total": 6
}
{
  "name": "Alice Johnson",
  "email": "alice@example.com",
  "phone": "9876543210",
  "role": "Admin"
}
{
  "name": "Alice J.",
  "phone": "9999999999"
}


join , @attribute , @orginal, @extra , rollback particular batch , static , change case in validator,
static in repository , file structure , validator file structure , err in logger , 
camelcase uniformly , 

